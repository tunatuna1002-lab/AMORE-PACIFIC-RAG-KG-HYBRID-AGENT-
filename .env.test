# Test Environment Configuration
# Usage: ENV_FILE=.env.test python -m pytest tests/

# Database paths (test-isolated)
DATABASE_PATH=./data/test_amore_data.db
KG_PATH=./data/test_knowledge_graph.json

# Disable external services in tests
GOOGLE_SHEETS_ENABLED=false
ENABLE_APIFY_SCRAPER=false

# Use mock LLM in tests (set to 'true' to skip actual API calls)
LLM_MOCK_MODE=false

# Optional: Use test API key if available
# OPENAI_API_KEY=sk-test-xxx
