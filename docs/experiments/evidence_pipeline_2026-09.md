# 증거 카드 · 규칙 추론 · ReAct 통합 — 단계별 결과 (2026-09)

> 공모전 이후 사후 작업(2026-09-17~). 지시서 `docs/plans/evidence-react-ontology-kickoff-prompt-2026-09-17.md`,
> 결정표 `docs/plans/evidence-react-ontology-decisions-2026-09.md`.
> 판정 규칙(지시서 §2): 평균 차이가 노이즈 기준(종합·검색 0.01, 근거성·관련성 0.03, 수치 정확도 0.05, 통과 수 5건)과
> 실제 실행 간 폭 중 큰 값 이상이고 실행 값 범위가 겹치지 않을 때만 "차이 있음".

## 0단계 — 정리와 측정 도구 수리

### 시작 상태 (2026-09-17)

- 브랜치 `feat/evidence-react-ontology-2026-09`를 `de90c05`(지시서 커밋)에서 생성.
- `data/chroma`: 백업 `data/chroma_backup_2026-09-17-pre-restore/`(1,145청크) → 787청크 삭제 → `amore_docs` **358**.

### 트랙별 파일 소유

| 트랙 | 소유 파일 | 비고 |
|---|---|---|
| 0-A 색인·조회 분리 | `src/rag/retriever.py`(초기화·색인부), `src/rag/retrieval_strategy.py`(`OWLRetrievalStrategy.__init__`, `create_owl_strategy`), 새 `src/rag/build_index.py`, `scripts/start.py`, 상태 API 라우트 | |
| 0-B 오류 가시화 | `src/rag/hybrid_retriever.py`(예외 처리부), `eval/runner.py`, `eval/schemas.py`, `eval/report.py`, `eval/brain_adapter.py`(`V4RetrievalTrace`·`_build_trace`) | |
| 0-C 비용 | `eval/cost_tracker.py`, `eval/schemas.py`·`eval/report.py`의 비용부 | 0-B와 같은 파일의 다른 영역 |
| 0-D 경로 관측 | `src/core/query_graph.py`, `src/core/graph_state.py`, `src/core/models.py`(Response), `src/core/brain.py:process_query` | 평가 리포트 연결은 0-B 병합 후 |
| 0-E 테스트 결함 | `tests/conftest.py`, `tests/unit/core/test_react_agent.py`, `tests/unit/core/test_cache.py` | |
| 1-A/1-B 시험지 | 새 `scripts/classify_golden_types.py`, `scripts/generate_rule_questions.py`, `eval/data/golden/typed/` | 코드 무관, 0단계와 병렬 |

### 시작 시 전체 테스트

- `de90c05` + chroma 복구 직후: **5,488 passed / 7 skipped / 0 failed** (403초). 이 실행에는 `test_react_run`의 실제 OpenAI 호출(F15)이 포함돼 있다.

### 병합 기록

| 트랙 | 커밋 | 내용 | 병합 전 리드 확인 |
|---|---|---|---|
| 0-D | `31040bf` | QueryGraph가 문항별 `route_trace`(route·confidence_level·score·구성 요소·도구) 기록 | diff 리뷰, 관련 테스트 72 passed |
| 0-C | `f7d9bcd` | 단가를 litellm `model_cost` 우선 조회(gpt-4.1-mini $0.40/$1.60), 리포트에 계층별 토큰·사용 단가 기록 | 1M+1M 토큰 → $2.00 확인 |
| 0-A | `3000d42` `49bc9f0` `368434a` `c349da9` | `DocumentRetriever.initialize()` 읽기 전용(`get_collection`만), 어긋남은 경고+`get_index_status()`, 색인 전용 `python -m src.rag.build_index`(`--dry-run`/`--prune`), `OWLRetrievalStrategy` `doc_retriever` 필수(F2), `scripts/start.py`가 서버 시작 전 색인(S0-2), `/api/v4/brain/status`에 `index_status` | 원본 복사본 dry-run: indexed 358 / expected 358 / in_sync True |
| 0-E | `c9db4e9` | `test_react_run` LLM 가짜화, 캐시 TTL 경계 버그(`>`→`>=`, 코드 결함) 수정, `tests/conftest.py` API 가드(더미 키 + 닫힌 base URL) | 가드 하에서 `test_llm_integration.py` 포함 50 passed |
| 2-A | `0ed7594` | 증거 카드 모델·어댑터·렌더러(새 파일만, 이 시점엔 호출처 없음 → 기준선 동작 불변) | 102 passed |
| 0-B | `024ad89` `f083519` | 검색 실패를 핵심(`retrieval_error` → 평가 인프라 실패)과 선택(`degraded`)으로 구분 기록, 19곳 판정 | 33 passed |
| 0-D2 | `f5eba7a` | route_trace를 평가 트레이스·리포트 집계(route·confidence 분포, 규칙 발화 문항)에 연결 | 변경 전 코드로 새 테스트 실행 → 8 failed(RED 확인) 후 병합 |
| 1-A/1-B | `d2a57a6` `3d39bd9` | 유형 분류·생성 문항(§1) | 생성 문항 3개 정답을 원본 DB로 재조회해 일치 확인 |

### 게이트 (2026-09-17 21:36)

- 전체 테스트(`9325a74`, `data/` 복사본이 있는 별도 worktree에서 — S0-7): **5,673 passed / 7 skipped / 0 failed** (369초). 실행 전후 원본 `data/` 파일 sha256 불변.
- `amore_docs` = 358 (0-A dry-run, 스모크·기준선 실행 후 복사본도 358).
- 오류 주입(`tests/unit/rag/test_retrieval_error_visibility.py`, `tests/eval/test_runner_retrieval_error.py`) · 비용(`tests/eval/test_cost_pricing.py`) · 경로 기록(`tests/unit/core/test_query_route_observation.py`, `tests/eval/test_route_trace_report.py`) 통과.
- 스모크 5문항(`0b39e02`): 채점 5 / 인프라 실패 0, route 전부 direct·high, 규칙 발화 0 — 알려진 결함이 그대로 관측됨.

### 비용 재계산 (0-C, 토큰 기반 — 기존 baseline 파일은 수정하지 않음)

입력·출력 단가가 같은 배율(8/3)로 바뀌므로 `재계산 = 기록 × 2.667`이 토큰 분할과 무관하게 정확하다. 리포트에는 L5(답변)+judge 토큰만 기록돼 있고 L2 임베딩·질의 확장(`DocumentRetriever.expand_query`, openai 클라이언트 직접 호출)은 어느 리포트에도 집계되지 않았다.

| 리포트 | 토큰 | 기록 비용 | 재계산 |
|---|---:|---:|---:|
| `eval/baselines/brain-v4-1.0-2026-09-17` | 1,203,879 | $0.3142 | $0.8379 |
| kg-full-run1/2/3 | 997,249 / 1,001,752 / 1,006,761 | $0.2547 / 0.2558 / 0.2581 | $0.6793 / 0.6821 / 0.6882 |
| kg-nokg-run1/2/3 | 808,865 / 808,112 / 824,234 | $0.2252 / 0.2262 / 0.2312 | $0.6005 / 0.6033 / 0.6164 |
| kg-norules-run1/2/3 | 1,008,482 / 1,009,674 / 999,796 | $0.2589 / 0.2594 / 0.2554 | $0.6904 / 0.6918 / 0.6810 |
| kg-reactowl-run1 | 4,226,054 | $0.6949 | $1.8531 |

## 1단계 — 유형별 시험지와 기준선

### 시험지 (1-A/1-B, `d2a57a6` `3d39bd9`)

- 분류: `scripts/classify_golden_types.py`(결정적 규칙 C1~C11, 문항별 근거 `eval/data/golden/typed/classification.jsonl`). 172문항 → numeric 53 · relation 8 · rule 10 · multihop 18(주 유형) · other 83(definition 9, document_explanation 34, document_general 18, ir_document 12, out_of_scope 7, forecast 3).
- 시험지에서 제외 23문항: 주 유형은 numeric·multihop이지만 `gold_source=domain_expectation`(추정 골드)이라 정답 채점 불가.
- 생성(`metadata.generated=true`, 근거 SQL·값·교차 확인 포함): rule 32(`scripts/generate_rule_questions.py`), relation 19(`generate_relation_questions.py`, `config/brands.json` 출처 트리플만), multihop 10(`generate_multihop_questions.py`, KG 관계 + DB 수치, 날짜 없는 KG 수치 엣지 미사용).
- 정답 수치 출처는 지표 테이블(결정 S1-1). 결론이 raw 재계산과 다른 후보는 제외(규칙 후보 결론 불일치 2, 브랜드 오귀속 4, multihop 3). 포함 문항의 두 출처 수치 차이: 39문항 중 최대 0.25(rg011 평균 순위 12 vs 9), 중앙값 0.029.
- 규칙 입력 공급 가능: 37개 중 13개 규칙만 DB·KG 입력이 있어 문항 생성에 12개 사용(sentiment 8·IR 5·이력 필요 규칙 불가).

| 유형 | 원본 | 생성 | 합계 |
|---|---:|---:|---:|
| ① numeric 수치 조회 | 31 | 0 | **31** |
| ② relation 관계 | 8 | 19 | **27** |
| ③ rule 규칙 판단 | 10 | 32 | **42** |
| ④ multihop 다단계 | 17 | 10 | **27** |

경계 문항 24개(목록은 `classification.jsonl`의 `boundary`): rule로 분류했지만 골드가 문서 서술인 9개(lg044·045·046·047·056·059·094·097·114), domain 라벨만으로 multihop 5개(lg155·156·159·192·195), 두 유형 기준 동시 충족 7개(lg051·074·082·109·194·196·099), other인데 rule 탐지기가 걸린 3개(lg111·121·122).

**측정 방식**: 통합 시험지 `eval/data/golden/typed/combined_v1.jsonl`(원본 172 + 생성 61 = 233)을 한 번 실행하고 `scripts/typed_eval_summary.py`로 유형별 부분집합을 같은 채점 코드로 재집계한다. 유형에 속하지 않는 106문항(other + 제외 23)은 회귀 확인용이다.

### 1-C 기준선 (`0b39e02`, 2026-09-17 21:37~21:59, 3회, 동시 3개·45초 간격, 실행별 Chroma 복사본)

조건: `--target v4 --data-as-of 2026-08-31 --judge llm --judge-model gpt-4.1-mini --semantic-similarity --concurrency 4`, 저장소 기본 플래그(ReAct OFF, OWL 전략 OFF, reranker OFF). 3회 모두 채점 233 / 인프라 실패 0 / degraded 0, 실행 후 Chroma 358. 저장: `eval/baselines/brain-v4-typed-1.0-2026-09-17`(종합 점수 중앙값인 1회차), 3회 리포트 `eval_output/evidence-2026-09/base-run{1,2,3}/`.

| 지표 (평균 [최소, 최대]) | ① numeric 31 | ② relation 27 | ③ rule 42 | ④ multihop 27 | 그 밖 106 | 전체 233 |
|---|---|---|---|---|---|---|
| 종합 점수 | 0.663 [0.658, 0.668] | 0.687 [0.679, 0.695] | 0.759 [0.754, 0.761] | 0.655 [0.650, 0.660] | 0.606 [0.604, 0.608] | 0.656 [0.655, 0.658] |
| 통과 수 | 0 [0, 0] | 0 [0, 0] | 2 [2, 2] | 0 [0, 0] | 8.7 [7, 10] | 10.7 [9, 12] |
| L2 개념 Recall | 0.597 | 0.981 | 0.905 | 0.716 | 0.504 [0.498, 0.509] | 0.668 [0.666, 0.671] |
| L3 엣지 Recall | 0.473 | 0.062 | 0.881 | 0.407 | 0.693 | 0.592 |
| L5 근거성 | 0.774 [0.755, 0.811] | 0.686 [0.610, 0.732] | 0.701 [0.696, 0.708] | 0.728 [0.711, 0.747] | 0.665 [0.660, 0.672] | 0.696 [0.687, 0.705] |
| L5 관련성 | 0.742 [0.737, 0.748] | 0.884 [0.856, 0.900] | 0.916 [0.902, 0.924] | 0.802 [0.785, 0.826] | 0.865 [0.860, 0.867] | 0.853 [0.842, 0.860] |
| L5 토큰 F1 | 0.142 [0.134, 0.147] | 0.150 [0.144, 0.155] | 0.216 [0.211, 0.219] | 0.184 [0.182, 0.187] | 0.123 [0.120, 0.125] | 0.152 [0.151, 0.154] |
| L5 수치 정확도 | 0.000 [0.000, 0.000] | 0.250 | 0.032 | 0.026 [0.019, 0.038] | 0.028 [0.000, 0.056] | 0.029 [0.028, 0.032] |
| 규칙 발화 문항 비율 | 0 | 0 | 0 | 0 | 0 | 0 |
| 규칙 정답 일치율 | — | — | 0.469 (생성 32문항 중 "발화하지 않음"이 정답인 15문항만 일치) | — | — | — |

| 분포 (3회 모두 동일) | 값 |
|---|---|
| route | direct 232, decide 1(relation 유형 1문항) — clarify·react 0 |
| confidence_level | high 232, medium 1 |
| 규칙 추론 | 발화 문항 0, 추론 0건 |
| ReAct | 0 (플래그 OFF) |

비용: 리포트 합 $3.232(답변+judge, 실제 단가) → 질의 확장·임베딩 미집계 보정 ×1.1 ≈ **$3.56**.

해석(추측 없이 말할 수 있는 것만): 네 유형 모두에서 경로가 사실상 한 가지(direct/high)라 신뢰도 분기·DecisionMaker·ReAct는 이 기준선에서 측정되지 않았다. 규칙은 한 번도 발화하지 않았고, 수치 조회 유형의 수치 정확도는 3회 모두 0이다(F8: v4 답변 프롬프트에 DB 수치가 없음). ③ rule의 종합 점수·관련성이 높은 것은 생성 문항에 골드 엣지·문서가 없어 L3·L2 게이트가 기계적으로 통과하는 영향이 섞여 있으므로 유형 간 비교에 쓰지 않는다.

## 2단계 — 증거 카드와 프롬프트 통합

### 병합 기록

| 트랙 | 커밋 | 내용 | 병합 전 리드 확인 |
|---|---|---|---|
| 3-A | `bf93f12` | 규칙 37개 입력 계약·미발화 사유 래퍼(`src/ontology/rule_contracts.py`). 호출처 없음 → 동작 불변 | 계약 단위 전수 확인(규칙 수정 없음) |
| 2-B | `622acb3` `fda11bf` | 특성화 테스트로 v1·v4 컨텍스트 섹션 고정 후, v4·v1 답변 프롬프트를 증거 카드 렌더러로 조립(`src/rag/evidence_assembly.py`), 카드 id 인용 지시 | 특성화 테스트 변경 전·후 통과 |
| 2-C | `b7b3ba5` | judge 근거성 컨텍스트 = 답변 프롬프트에 실린 카드 집합 | |
| 0-F | `818a1cc` | 테스트가 운영 `data/`를 쓰지 않게 격리(S0-7 후속) | |
| 3-C | `919b9e6` | 규칙 관측(발화 규칙·미발화 사유·규칙 정답 일치율)을 리포트 필드로 | |

### 측정 (`b7b3ba5`, 2026-09-17 22:28~22:56, 3회, 결정 S2-1의 worktree + 동결 스냅샷)

조건은 1-C 기준선과 같다(ReAct·OWL·reranker OFF). `scripts/typed_eval_summary.py`로 유형별 재집계. 판정은 §2 규칙.

| 지표 (평균 [최소, 최대]) → s2−base 판정 | ① numeric 31 | ② relation 27 | ③ rule 42 | ④ multihop 27 | 그 밖 106 | 전체 233 |
|---|---|---|---|---|---|---|
| 종합 점수 | 0.729 [0.727, 0.731] +0.067 **有** | 0.719 [0.715, 0.724] +0.032 **有** | 0.812 [0.810, 0.814] +0.053 **有** | 0.721 [0.714, 0.724] +0.066 **有** | 0.632 [0.630, 0.634] +0.026 **有** | 0.698 [0.696, 0.699] +0.042 **有** |
| 통과 수 | 7.0 [7, 7] +7.0 **有** | 0 [0, 0] 무 | 17.7 [16, 19] +15.7 **有** | 2.3 [2, 3] +2.3 무 | 19.3 [17, 22] +10.7 **有** | 46.3 [43, 50] +35.7 **有** |
| L2 개념 Recall | 0.586 −0.011 무 | 0.981 무 | 0.905 무 | 0.722 무 | 0.492 −0.012 무 | 0.662 무 |
| L3 엣지 Recall | 0.473 무 | 0.062 무 | 0.881 무 | 0.411 무 | 0.693 무 | 0.593 무 |
| L5 근거성 ※ | 0.956 [0.949, 0.968] +0.183 | 0.923 [0.892, 0.942] +0.237 | 0.907 [0.893, 0.923] +0.206 | 0.882 [0.858, 0.928] +0.154 | 0.912 [0.909, 0.914] +0.247 | 0.915 [0.912, 0.917] +0.219 |
| L5 관련성 ※ | 0.937 +0.194 | 0.926 +0.042 무 | 0.999 +0.083 | 0.991 +0.189 | 0.911 +0.047 | 0.941 +0.089 |
| L5 토큰 F1 | 0.222 [0.212, 0.228] +0.080 **有** | 0.154 +0.004 무 | 0.287 +0.072 **有** | 0.262 +0.078 **有** | 0.125 +0.002 무 | 0.186 +0.034 **有** |
| L5 수치 정확도 | 0.700 [0.667, 0.717] +0.700 **有** | 0.750 +0.500 **有** | 0.683 [0.661, 0.694] +0.651 **有** | 0.616 [0.587, 0.630] +0.590 **有** | 0.046 [0, 0.083] +0.019 무 | 0.568 [0.558, 0.581] +0.539 **有** |
| 규칙 발화 문항 비율 / 규칙 정답 일치율 | 0 | 0 | 0 / 0.469 (불변) | 0 | 0 | 0 |

※ 근거성·관련성은 수치상 모든 유형에서 "차이 있음" 기준을 넘지만, 2-C가 judge 컨텍스트 자체를 바꿨으므로(기준선: 답변 프롬프트에 없던 DB 사실이 judge에만 들어감 → 2단계: 프롬프트 카드와 같은 집합) **답변 개선과 채점 조건 변경이 섞인 값**이다. 순수한 답변 개선의 근거로는 judge와 무관한 수치 정확도·토큰 F1만 쓴다. 교차 채점(`scripts/judge_context_swap.py`)으로 분리하지는 않았다(비용 우선순위).

분포: route·신뢰도는 기준선과 같다(direct/high 232, decide/medium 1). 규칙 발화 0 — 3-B 미병합 상태라 예상대로.

**인프라 실패 2건 (원인 확인)**: run2 `lg161`, run3 `lg155`(둘 다 multihop). 로그상 답변 생성은 끝났고 judge 호출이 `Judge call timed out after 60s`를 3회 연속 내 채점에서 제외됐다(`scoring_error`). 세 실행의 judge 시간 초과 12건 중 9건이 22:42~22:45 3분 안에 세 프로세스에서 동시에 발생 → 외부 API 지연으로 판단. 기준선 3회의 시간 초과는 3건(모두 재시도 성공). 2단계 judge 입력이 문항당 약 +620토큰 길어진 것이 시간 초과 빈도에 기여했는지는 확인하지 않았다(추측). 제외 문항 때문에 run2·run3의 multihop·전체 집계는 26·232문항 기준이다.

**비용·지연**

| | 답변(L5) 입력 토큰/문항 | judge 입력 토큰/문항 | 리포트 비용/회 | 평균 지연 | 중앙값 | p90 |
|---|---:|---:|---:|---:|---:|---:|
| 기준선 3회 평균 | 748 | 4,354 | $1.077 (L5 $0.170) | 5.86s | 5.67s | 8.0s |
| 2단계 3회 평균 | 3,342 (**×4.5**) | 4,968 | $1.434 (L5 $0.431, ×2.5) | 7.60s (+30%) | 6.87s | 11.0s |

2단계 3회 리포트 합 $4.302 → ×1.1 ≈ **$4.73**.

### 게이트 판정: 통과

- ① numeric 수치 정확도 0.000 → 0.700(범위 비겹침), 통과 수 0 → 7 — 노이즈 기준 이상 개선.
- 다른 유형 근거성 회귀 없음(모두 상승. 단 위 ※ 조건 변경 주의).
- 해석: F8(v4 답변 프롬프트에 DB 수치 없음)이 수치 정확도 0의 주원인이었다는 가설과 일치한다. ③ rule 통과 2 → 17.7은 규칙 추론이 아니라(발화 0) 답변 LLM이 metric 카드를 보고 직접 판단한 결과다 — 규칙 엔진의 기여는 3단계에서 따로 잰다. 대가는 답변 입력 토큰 ×4.5, 평균 지연 +30%.
- 남은 과제: 답변 속 수치가 인용 카드와 실제로 일치하는지는 judge가 아니라 규칙 기반 검증기(2-D, E8)로 확인한다.

## 3단계 — 규칙 추론 살리기

### 병합 기록

| 트랙 | 커밋 | 내용 | 병합 전 리드 확인 |
|---|---|---|---|
| 2-D | `b1115d5` `6d95460` `d587178` | 답변 수치 검증기(E8 뒷부분): 문장 속 수치를 인용 카드 값과 대조해 verified/no_citation/mismatch/unknown_card로 분류. 플래그 `response.numeric_verification_mode` 기본 **annotate**(답변 불변, 메타데이터만) | 게이트 테스트 RED(17 failed) 확인, 관련 테스트 2,430 통과 |
| 2-D 후속 | `7d4c634` | 검증 결과를 평가 트레이스·리포트·유형별 요약에 연결 | 새 테스트 6건 RED 확인 후 병합 |
| 3-B | `7268e64` `1e42d78` | 규칙 추론 입력을 대시보드 JSON(`current_metrics`)에서 **증거 카드**로 교체(F7·E3). 추론 결과는 `derived_from`이 달린 inference 카드. `MetricFactsProvider`에 cpi·avg_rating_gap·brand_avg_rank 추가, `reasoner.apply`가 결론 필드 보존, 소유 규칙 대소문자 결함 수정 | 게이트 테스트 RED(8 failed/1 passed) 확인, 관련 테스트 3,625 통과 |

게이트 전체 테스트(`1e42d78`): **6,091 passed / 7 skipped / 0 failed**. 실행 중 `data/chroma/chroma.sqlite3` 해시가 바뀌었으나 내용 테이블(embeddings·embedding_metadata·collections·segments·max_seq_id·embeddings_queue·migrations)은 동결 스냅샷과 동일하고 Chroma 내부 잠금 테이블 `acquire_write`만 달랐다(`amore_docs`=358). 결정 S3-2.

### 측정 (`1e42d78`, 2026-09-18 03:37~04:08, 통합 시험지 3회 + 규칙 off 3회)

규칙 off = `FF_REASONER_USE_OWL_REASONER=false FF_REASONER_USE_UNIFIED_REASONER=false`, ③ rule 시험지 42문항만(결정 S3-1). 그 밖 조건은 1-C·2단계와 같다.

**③ rule 42문항 — 기준선/2단계/3단계**

| 지표 | 기준선 | 2단계 | 3단계 | 3단계−기준선 | 3단계−2단계 |
|---|---|---|---|---|---|
| 규칙 발화 문항 비율 | 0 | 0 | **0.568** | +0.568 有 | +0.568 有 |
| 규칙 정답 일치율 | 0.469 | 0.469 | **0.779** | +0.310 有 | +0.310 有 |
| 통과 수 | 2.0 | 17.7 | **23.7** | +21.7 有 | +6.0 有 |
| L5 수치 정확도 | 0.032 | 0.683 | **0.777** | +0.745 有 | +0.094 有 |
| L5 근거성 | 0.701 | 0.907 | 0.966 | +0.265 有 | +0.059 有 |
| 종합 점수 | 0.759 | 0.812 | 0.800 | +0.041 有 | −0.012 有 |

오프라인(LLM 없이) 측정한 규칙 일치율 25/32 = 0.781과 평가 실측 0.779가 일치한다.

**규칙 on vs off (rule 42문항, 각 3회)**

| 지표 | 규칙 on | 규칙 off | 차이 |
|---|---|---|---|
| 규칙 정답 일치율 | 0.779 [0.774, 0.781] | 0.469 [0.469, 0.469] | **+0.310 (범위 비겹침)** |
| 발화 문항 / 추론 수 | 23.7 / 116.7 | 0 / 0 | — |
| 통과 수 | 23.7 [23, 24] | 22.3 [21, 25] | +1.3 (노이즈 5건 미만 → 차이 없음) |
| 종합 점수 | 0.800 [0.797, 0.803] | 0.822 [0.816, 0.827] | **−0.022 (범위 비겹침 → 규칙 on이 더 낮음)** |
| L5 수치 정확도 | 0.777 [0.758, 0.790] | 0.790 [0.790, 0.790] | −0.013 (노이즈 0.05 미만) |
| L5 근거성 | 0.966 | 0.953 | +0.014 |
| 비용 / 지연 | $0.244 / 6.4s | $0.232 / 7.1s | +5% / − |

해석(확인된 것만): 규칙 엔진은 **규칙 판단의 정답 일치율을 0.469 → 0.779로 올린다**(같은 문항·같은 데이터, 범위 비겹침). 반면 judge 종합 점수는 규칙 off가 0.022 높다 — 유리하게 다듬지 않고 그대로 적는다. 종합 점수는 L1~L5 가중 합이라 규칙 발화가 만드는 추가 문장이 관련성·F1 채점에서 손해를 볼 수 있으나, 원인은 확인하지 않았다(추측).

**다른 유형에 생긴 부작용 (3단계−2단계)**

| 유형 | 종합 | L2 개념 Recall | L5 수치 정확도 | 규칙 발화 문항 비율 |
|---|---|---|---|---|
| ① numeric 31 | −0.028 有 | +0.006 무 | −0.060 有 | 0 → 0.609 |
| ② relation 27 | −0.028 有 | 0.000 무 | −0.167 (범위 겹침) | 0 → 0.852 |
| ④ multihop 27 | −0.023 有 | −0.036 有 | −0.044 무 | 0 → 0.731 |
| 그 밖 106 | −0.021 有 | −0.048 有 | −0.028 무 | 0 → 0.453 |
| 전체 233 | −0.021 有 | −0.028 有 | −0.011 무 | 0 → 0.572 |

원인을 확인했다: **규칙 추론 결과가 질의 확장(`_expand_query`)에 들어가 문서 검색 결과를 바꾼다.** 문서 카드 수는 그대로인데(2단계 8.00개 → 3단계 7.93개) 검색된 청크가 달라졌다 — 같은 설정 두 실행의 문서 겹침이 0.91인데, 2단계 대비 3단계 겹침은 추론이 발화한 문항 132개에서 **0.472**, 발화하지 않은 문항 101개에서 0.906이었다. 즉 규칙 발화가 문서 검색을 바꾼 것이지 카드 자리 경쟁이 아니다. 프롬프트는 문항당 3,357 → 3,671토큰(+9%).

**답변 수치 검증(2-D, annotate — 답변은 바꾸지 않음), 3회 합계**

| 분류 | 합계(3회, 문항당 평균 9.2개 수치) | 비고 |
|---|---|---|
| verified (인용 카드 값과 일치) | 2,046 (31.9%) | |
| no_citation (그 문장에 카드 인용 없음) | 3,944 (61.5%) | 이 중 **97.9%**(3,898)는 인용은 없지만 프롬프트의 다른 카드에는 있는 값 |
| mismatch (인용 카드 값과 다름) | 394 (6.1%) | 카드 값으로 계산한 수(차이·배수·계단)가 섞여 있다 |
| unknown_card (없는 카드 id 인용) | 35 (0.5%) | |

즉 인용 지시를 넣었지만 모델은 문장마다 카드 id를 붙이지 않는다(61.5%). 다만 그 수치들도 대부분 프롬프트 카드에 있는 값이라, 카드에 없는 값을 지어낸 비율은 6.6%(mismatch+unknown_card) 수준이다. enforce로 올리면 이 6.6% 중 계산으로 얻은 수치까지 "확인되지 않음"으로 바뀌므로 6단계에서 판정한다.

**인프라 실패**: run1 lg155·rg013, run2 lg048·lg155, run3 lg155 — 전부 judge 호출 60초 시간 초과 3회. `lg155`는 3회 모두 실패했다(2단계에서도 1회 실패) → 문항 고유의 긴 judge 입력이 원인일 가능성이 높다(미검증). FUTURE_WORK에 재시도 간격·컨텍스트 길이 문제로 기록.

**비용·지연**: 리포트 합 $5.31(233문항 3회 $4.614 + rule off 3회 $0.695) → ×1.1 ≈ **$5.84**. 평균 지연 7.0s(2단계 7.6s, 기준선 5.9s — 단, 3단계 실행 중에는 다른 작업이 함께 돌아 비교에 주의).

### 게이트 판정: 통과

- DB 픽스처 통합 테스트에서 시장 포지션 규칙(`market_dominance_fragmented`) 발화 + `derived_from` 확인 (`tests/unit/rag/test_rule_inference_retrieve.py`).
- ③ rule 시험지 3회에서 발화율 0.568 > 0, 규칙 정답 일치율 0.779 기록.
- 규칙 off 대비: 정답 일치율 +0.310(판정 "차이 있음"), 종합 점수 −0.022(판정 "차이 있음", 규칙 on이 불리) — 무효과가 아니라 **지표에 따라 방향이 갈린다**는 사실을 그대로 남긴다.
- 후속(4·5단계에서 볼 것): 규칙 결과의 질의 확장 반영이 다른 유형의 검색을 흔든다 — 확장에 쓰는 추론을 제한할지 판단이 필요하다.
